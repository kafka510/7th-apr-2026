"""
Solar Insight services (parsers, validators, KML, KPI, layout, PDF) for Engineering Tools.

Import submodules directly to avoid loading optional heavy dependencies until needed, e.g.:
  from engineering_tools.solar_services.yield_engine import calculate_yield
  from engineering_tools.solar_services.layout_kml import run_layout_export
"""
